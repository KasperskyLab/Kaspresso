apply plugin: 'kotlin'
apply plugin: 'application'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    compile "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"
    compile project(":command_handler")
    compile project(":host_connection_contract")
}

mainClassName = "com.kaspersky.uitest_framework.hostconnection.HostConnectionClientKt"
sourceCompatibility = "7"
targetCompatibility = "7"

buildscript {
    ext.kotlin_version = '1.3.11'
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}
repositories {
    mavenCentral()
}

task fatJar(type: Jar) {
    baseName = 'HostConnectionClient'
    with jar
    manifest { attributes 'Main-Class': 'com.kaspersky.uitest_framework.hostconnection.HostConnectionClientKt' }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}
project.tasks["fatJar"].dependsOn(assemble)